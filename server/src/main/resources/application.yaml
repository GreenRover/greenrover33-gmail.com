spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  properties:
    hibernate:
      dialect: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    initialization-mode: never
    platform: postgres
    url: "jdbc:postgresql://db-postgresql-fra1-39545-do-user-2658320-0.b.db.ondigitalocean.com:25060/db1"
    username: user
    password: mtivwpb48y98e91v
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://dev-adhggyp0.eu.auth0.com/
        audience: http://localhost:4200

server:
  port: 8038

logging:
  level:
    root: INFO
    com:
      example: ${LOG_LEVEL:INFO}
    org:
      springframework:
        web: ${LOG_LEVEL:INFO}
        http:
          client: ${LOG_LEVEL:INFO}
  
  
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always